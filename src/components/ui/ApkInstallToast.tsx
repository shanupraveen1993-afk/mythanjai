"use client";

import React from "react";

/**
 * ApkInstallToast: Floating popup disabled as per user instruction.
 * Static APK Download banners are now integrated directly in Home & Landing pages.
 */
export default function ApkInstallToast() {
  return null;
}

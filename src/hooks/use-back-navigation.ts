"use client";

import { useEffect } from "react";
import { usePathname, useRouter } from "next/navigation";

export function useBackNavigation() {
  // Standardized back navigation handled cleanly via native router history
  return null;
}
